
var game = new Phaser.Game(phaserConfig);

window.addEventListener('resize', function (event) {
    myResize(game);
}, false);


//window.onload = () => {
//    var game = new Controller();
//};

c.width |= c.style.background = "#CDF"; 
for (j = 3e3; j--; x.arc(960, 540, 430 + 60 * S(j / 500 + a * 4) * (S(a - t) / 2 + .5) ** 9, a, a))
a = j / 159 + t, x.lineWidth = 29
x.stroke()